#include <stdio.h>

/**
 * main - gives the command name
 * @argc: argument count
 * @argv: array of arguments strings
 * Return: 0
 */

int main(int argc, char **argv)
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
